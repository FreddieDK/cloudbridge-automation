---
- name: Test sudo
  hosts: linux
  gather_facts: false
  become: true
  become_method: sudo
  become_user: root

  tasks:
    - name: Check effective user
      ansible.builtin.command: whoami
      register: whoami

    - name: Show result
      ansible.builtin.debug:
        var: whoami.stdout